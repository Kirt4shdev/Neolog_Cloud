# Production environment for Docker
# Nginx will proxy all /api, /unprotected, /admin, /common requests to backend
# So we use relative URLs (empty host/port) or the same domain
VITE_BACKEND_HOST=
VITE_BACKEND_PORT=

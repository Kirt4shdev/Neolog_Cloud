# API
API_PORT=8080
API_JWT_SECRET_TOKEN=change_this_secret_in_production_neologg_cloud_2026
API_ALLOWED_CORS_ORIGINS=http://localhost:5174,http://localhost:5173

# PostgreSQL
POSTGRES_CONNECTION_STRING=postgresql://postgres:postgres@postgres:5432/neologg_cloud_db

# SMTP (valores de ejemplo - configurar según necesidad)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_FROM_ADDRESS=noreply@neologg.com
SMTP_USERNAME=noreply@neologg.com
SMTP_PASSWORD=changeme

# Valkey/Redis
VALKEY_HOST=valkey
VALKEY_PORT=6379
VALKEY_PASSWORD=change_me_to_strong_password_32_chars

# Crypto
CRYPTO_TOOL_PASSWORD=neologg_crypto_password_change_in_production
CRYPTO_TOOL_ALGORITHM=aes-256-ctr

# Dev
SHOW_DEV_LOGS=true
EXECUTE_MODE=dev
DEBUG=backend:*

# Admin Auth
ADMIN_AUTHORIZATION_HEADER=Bearer admin_secret_token_change_in_production

# Neologg Cloud - MQTT
MQTT_HOST=mosquitto
MQTT_PORT=1883
MQTT_USERNAME=neologg
MQTT_PASSWORD=neologg93
MQTT_PROTOCOL=mqtt

# Neologg Cloud - InfluxDB v2
INFLUXDB_URL=http://influxdb:8086
INFLUXDB_TOKEN=neologg93token_change_this_in_production
INFLUXDB_ORG=neologg
INFLUXDB_BUCKET=neologg_data

# Neologg Cloud - Mosquitto Management
MOSQUITTO_PASSWD_FILE=/mosquitto/data/passwd
MOSQUITTO_ACL_FILE=/mosquitto/data/acl
MOSQUITTO_CONTAINER_NAME=neologg_cloud_mosquitto
